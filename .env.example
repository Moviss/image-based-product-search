# MongoDB connection string (required)
MONGODB_URI=mongodb+srv://user:password@host/database

# Anthropic API key (optional — only required for promptfoo evaluations: npm run eval, npm run eval:redteam)
# The application itself receives the API key at runtime via the UI, not from this file.
ANTHROPIC_API_KEY=sk-ant-...
